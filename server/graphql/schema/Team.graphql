type Team {
  id: ID!
  owner: User!
  members: [User!]!
  channels: [Channel!]!
}

type CreateTeamResponse {
  ok: Boolean!
  team: Team
  errors: [Error!]
}

type Mutation {
  createTeam(name: String!): CreateTeamResponse!
  deleteTeam(id: ID!): Boolean!
}